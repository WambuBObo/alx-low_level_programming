#include "main.h"
/**
 * _isupper - function that checks for uppercase letters
 * @c: int type number
 * Return: 1 if uppercase, else 0
 */
int _issuper(int c)
{
	if ((c >= 'A') && (c <= 'Z'))
		
	return (1);
	else

	return(0);
}
